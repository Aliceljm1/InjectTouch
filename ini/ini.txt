USE_SEND_INPUT	=	1 		# 是否采用鼠标模拟
ZOOM			=	NORMAL 	# 缩放方式，NORMAL为不采取任何措施，原封不动模拟点；【ZOOMIN、ZOOMOUT】
TOUCH_ID		=	[] # 启用的ID，touchinfo一共9个[0,8]
TOUCH_NUM		=	1 		# 启用的指数，如果TOUCH_ID不为空，优先使用TOUCH_ID里面的个数
TIMEOUT			=	3000 	# 启动程序到开始模拟的睡眠时间，毫秒
OFFSET_X		=	0 		# 整体点x的偏移，输入(0, 100)，模拟的实际上是(0+OFFSET_X, 100+OFFSET_Y)，可以为负数
OFFSET_Y		=	0 		# 整体点y的偏移，可以为负数
DELAY			=	0		# 整体点增加睡眠时间，毫秒，可以为负数，计算结果最低为睡眠0秒
DELAY_MUL		=	0		# 睡眠时间增加的倍数，和DELAY一起作用，且DELAY优先计算，倍数最小为1